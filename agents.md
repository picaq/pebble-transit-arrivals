# Agents Guide

This repository uses `CLAUDE.md` as the primary agent guidance document.

For agent-specific instructions, read:

- `CLAUDE.md`

If you are using an agent workflow, this file exists so the agent can find the same guidance by name.
